{
    handle /api/* {
        uri strip_prefix /api
        reverse_proxy backend:8080
    }

    handle {
        root * /usr/share/caddy
        file_server
        try_files {path} /index.html
    }
}
